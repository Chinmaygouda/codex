[project]
name = "codecourt"
version = "0.1.0"
description = "Grounded debate-driven evaluation for AI code agents"
requires-python = ">=3.12"
dependencies = [
  "openai>=2.38.0",
  "python-dotenv>=1.0.0",
  "google-genai>=1.0.0",
]

[tool.unittest]
start-directory = "tests"
